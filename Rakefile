task :default => :compile

desc "Compiles the inbound_agi_tunnel project."
task :compile do
  system "cd inbound_agi_tunnel/src; erlc *.erl"
end

desc "Compiles and runs the inbound_agi_tunnel"
task :run => [:compile] do
  abort "TODO"
end
